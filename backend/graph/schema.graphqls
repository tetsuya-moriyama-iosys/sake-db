# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Message {
  id: ID!
  message: String!
}

type Query {
  messages: [Message!]!
}

input NewMessage {
  message: String!
}

type Mutation {
  createMessage(input: NewMessage!): Message!
}